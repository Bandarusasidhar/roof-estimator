# AI Log

AI tools were used during development as an implementation and debugging aid.

I used ChatGPT to help with project structure, React and Express implementation, MongoDB integration, API wiring, validation, CSS improvements, owner-panel functionality, Git setup, and troubleshooting during development.

AI was used interactively rather than treating generated code as automatically correct. I tested the application locally after changes and adjusted the implementation when the generated output did not match the existing project structure or the assignment requirements.

One specific example was the owner-panel/frontend work. An earlier generated version did not match the existing project structure and styling closely enough, so I stopped using that version and reworked the implementation around the actual files already present in the project. The final application was then tested through the public estimator and owner panel.

I also verified the MongoDB connection, backend API responses, owner authentication, estimator submission, estimate response, and captured-lead flow rather than assuming the generated code worked.

The substantial application work includes the React estimator flow, backend API integration, MongoDB-backed configuration and lead handling, owner authentication flow, owner configuration interface, lead display, validation, and responsive styling. AI assisted with implementation and troubleshooting, but the generated suggestions were reviewed and adapted to the actual project and assignment requirements.